// PostCSS config — this is the pipeline that processes CSS before the browser sees it
// tailwindcss: adds utility classes from Tailwind
// autoprefixer: adds vendor prefixes (like -webkit-) for older browsers
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
